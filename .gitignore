*.env
*.pth

# Python bytecode / caches
__pycache__/
*.pyc
*.pyo

# Local backups / snapshots
*.bak

# Runtime data (per-user RAG indexes, transcript DB, KG snapshots)
rag_indexes/
*.faiss
sessions.db
kg_state.json
kg_culture.json
faces.npz
*.npz
# Culture selector runtime sidecars (written by the loop/viz, not source)
active_state.json
culture_override.json
